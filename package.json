{
  "name": "fisaval",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev": "npm run dev --prefix app",
    "dev:api": "npm run dev --prefix api",
    "dev:all": "concurrently -n api,app -c blue,green \"npm run dev:api\" \"npm run dev\"",
    "build": "npm run build:pages --prefix app",
    "build:pages": "npm run build:pages --prefix app && node scripts/copy-to-docs.mjs",
    "typecheck": "npm run typecheck --prefix app",
    "reset:demo": "node scripts/reset-demo.mjs",
    "preview:lan": "npm run build --prefix app && npm run preview:lan --prefix app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
