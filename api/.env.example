PORT=8790
MUNICIPIO_NOME=Prefeitura Municipal (demo)
TENANT_ID=demo
# MULTI_TENANT=true
# TENANTS=demo:Prefeitura de Exemplo;paulinia-sp:Prefeitura de Paulínia
# SUPER_ADMIN_EMAILS=admin@demo
# ASSINATURA_MODOS=canvas,icp,govbr
# ASSINATURA_PADRAO=canvas
# ALERTA_PRAZO_MIN=1
JWT_SECRET=troque-este-segredo-em-producao
JWT_EXPIRES=7d
UPLOADS_DIR=uploads
# CORS_ORIGIN=https://rivascode-ops.github.io

# Web Push (opcional): node scripts/generate-vapid.mjs
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:gestor@prefeitura.local
# DATA_DIR=/caminho/persistente

# Roteirização VROOM (opcional): http://127.0.0.1:3000
# VROOM_URL=

# Check-in: raio máximo em metros (0 = desliga validação)
CHECKIN_RADIUS_M=200
# Limite de OS ativas por fiscal (0 = sem limite)
# MAX_OS_ATIVAS_FISCAL=15
# MAX_VISITAS_DIA_FISCAL=12

# Dados JSON isolados por tenant em data/tenants/{TENANT_ID}/ (padrão ligado)
# TENANT_ISOLATED=0

# Sem DATABASE_URL = banco JSON local (fase 2)
# Com PostGIS (fase 3):
# DATABASE_URL=postgres://fisaval:fisaval@localhost:5432/fisaval
